{
  "name": "manhwa-zone-project",
  "version": "1.0.0",
  "description": "Manhwa Server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",  // ⬅️ Vercel က ဒီ command ကို သုံးပါတယ်။
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.0.3",
    "multer": "^1.4.5-lts.1"
  }
}
